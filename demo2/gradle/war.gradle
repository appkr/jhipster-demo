apply plugin: "war"

bootWar {
    mainClassName = "dev.appkr.demo2.Demo2App"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
