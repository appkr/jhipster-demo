apply plugin: "war"

bootWar {
    mainClassName = "dev.appkr.uaa.UaaApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
